# _NON TESTED_

=== ИЗМЕНЕНИЯ В ТЕСТОВОЙ ВЕТКЕ ===

1. Удаление функционала маршрутизации .RU сайтов:
   - Из функции `manage_configs()` полностью удален пункт меню "3) Включить/Выключить маршрутизацию .RU сайтов мимо VPN".
   - Соответственно, сдвинута нумерация в меню `manage_configs` (пункт "Пересоздать конфиги" стал номером 3).
   - Из главного меню обновлено название 4-го пункта: теперь "4) Настройка конфигов (Порты / FP)" вместо "(Порты / .RU сайты / FP)".
   - Из функции `generate_xray_config()` удален блок кода, генерирующий правило `ROUTE_RU_RULE` (для доменов .ru, .su, .рф и geosite:ru).
   - В шаблоне `/usr/local/etc/xray/config.json` удалена подстановка `$ROUTE_RU_RULE` в блоке `routing`.
   - Переменная `ROUTE_RU_DIRECT` полностью вычищена из логики скрипта: она больше не запрашивается, не сохраняется в файл `/etc/xray_admin.conf` и не проверяется при старте панели.

2. Изменения в главном меню:
   - Пункт "10) Удалить сервер" переименован в "10) Удалить xray" для более точного отражения сути действия (скрипт удаляет ядро и настройки, а не саму VPS).
